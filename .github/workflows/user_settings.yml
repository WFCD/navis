name: user_settings

on:
  pull_request:
    paths:
      - "packages/user_settings/**"
      - ".github/workflows/market_repo.yaml"

jobs:
  build:
    defaults:
      run:
        working-directory: packages/user_settings

    runs-on: ubuntu-latest

    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      min_coverage: 90
