name: permissions_client
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.3
