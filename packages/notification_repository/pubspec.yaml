name: notification_repository
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  firebase_core: ^3.9.0
  firebase_messaging: ^15.2.5
  flutter:
    sdk: flutter
  intl: ^0.20.1
  warframestat_client: ^4.2.8

dependency_overrides:
  meta: 1.15.0
  web: 0.3.0

dev_dependencies:
  very_good_analysis: ^7.0.0
