name: notification_repository
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  firebase_core: ^3.14.0
  firebase_messaging: ^15.2.6
  flutter:
    sdk: flutter
  intl: ^0.20.1
  warframestat_client: ^4.2.10

dependency_overrides:
  meta: 1.15.0
  web: 0.3.0
