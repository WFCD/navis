import 'package:flutter/material.dart';

class NightwavePage extends StatelessWidget {
  const NightwavePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
